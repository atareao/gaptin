gaptin
======

gaptin is a graphical interface to install apps with apt install

Download
--------

Download the [package][dlp] *or* checkout the source:

    $ git clone https://github.com/atareao/gaptin


Installation
------------

    $ sudo add-apt-repositoy ppa:atareao/gaptin
    $ sudo apt-get update
    $ sudo apt-get install gaptin
